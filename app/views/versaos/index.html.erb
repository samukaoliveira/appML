<p style="color: green" class="text-center"><%= notice %></p>

<h1>Versaos</h1>

<div id="versaos">
  <% @versaos.each do |versao| %>
    <%= render versao %>
    <p>
      <%= link_to "Show this versao", versao %>
    </p>
  <% end %>
</div>

<%= link_to "New versao", new_versao_path %>
