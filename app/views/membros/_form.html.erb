<%= form_with(model: membro) do |form| %>
  <% if membro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membro.errors.count, "error") %> prohibited this membro from being saved:</h2>

      <ul>
        <% membro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
<div style="display: none;">
  <%= form.label :ministerio_id, style: "display: block" %>
  <%= form.hidden_field :ministerio_id, value: @membro.ministerio_id %>
</div>

  <div>
    <%= form.label :usuario_id, style: "display: block" %>
    <%= form.select :usuario_id, value: opcoes_de_membro %>
  </div>

  <div>
    <%= form.submit "Salvar", class: "btn btn-success" %>
  </div>
<% end %>
