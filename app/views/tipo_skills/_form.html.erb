<%= form_with(model: tipo_skill) do |form| %>
  <% if tipo_skill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_skill.errors.count, "error") %> prohibited this tipo_skill from being saved:</h2>

      <ul>
        <% tipo_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
